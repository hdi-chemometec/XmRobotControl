"use strict";
/*let robotState: RobotState = RobotState.NOT_STARTED;

function updateRobotState(newState: RobotState) {
    robotState = newState;
}

export enum RobotState {
    NOT_STARTED = "NOT_STARTED",
    PICKING_UP_TIP = "PICKING_UP_TIP",
    ASPIRING_SAMPLE = "ASPIRING_SAMPLE",
    DISPENSING_SAMPLE = "DISPENSING_SAMPLE",
    DROPPING_TIP = "DROPPING_TIP",
    FINISHED = "FINISHED"
}*/ 
